import { Metadata } from "next";

export default {
    title: "Acme",
    description: "Describe the app function and usage.",
    icons: {
        icon: "/assets/favicon.ico",
    }
} satisfies Metadata;